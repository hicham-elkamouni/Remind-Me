import * as React from 'react';
import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';
import { Enctypo } from '@expo/vector-icons';

export const Checkbox = ({
    id,
    text,
    isCompleted,
    isToday,
    hour
}) => {
  return (
    <TouchableOpacity>
      {}
    </TouchableOpacity>
  )
}
